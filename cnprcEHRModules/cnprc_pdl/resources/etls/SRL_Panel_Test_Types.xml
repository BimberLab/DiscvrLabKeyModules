<?xml version="1.0" encoding="UTF-8"?>
<etl xmlns="http://labkey.org/etl/xml">
    <name>SRL Panel Test Types</name>
    <description>Panel Test Types</description>
    <transforms>
        <transform id="step1" type="org.labkey.di.pipeline.TransformTask">
            <description>Copy to target</description>
            <source schemaName="cnprcSrc_srl" queryName="q_panel_test_types" />
            <destination schemaName="lists" queryName="Panel Test Types" bulkLoad="true" batchSize="500" targetOption="truncate">
            </destination>
        </transform>
    </transforms>
    <schedule>
        <!-- run at 1 am everyday -->
        <cron expression="0 0 1 * * ? *"/> <!--seconds, minutes, hours, day-of-month, month, day-of-week, year(optional) -->
    </schedule>
</etl>