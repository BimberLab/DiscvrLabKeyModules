{
  "name": "jbrowse-lk",
  "version": "0.0.0",
  "scripts": {
    "clean": "bnr clean",
    "build": "npm run clean",
    "build-prod": "npm run build",
    "prebuild": "npm run clean",
    "setup": "npm install"
  },
  "betterScripts": {
    "clean": {
      "command": "rimraf resources/web/jbrowseApp"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "better-npm-run": "0.1.0",
    "jbrowse": "github:GMOD/jbrowse",
    "rimraf": "2.6.2"
  }
}
