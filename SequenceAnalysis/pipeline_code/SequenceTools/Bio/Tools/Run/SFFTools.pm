#
# BioPerl module for Bio::Tools::Run::SFFTools
#
# Please direct questions and support issues to <bioperl-l@bioperl.org>
#
#
# You may distribute this module under the same terms as perl itself

# POD documentation - main docs before the code

=head1 NAME

Bio::Tools::Run::SFFTools - a run wrapper for Roche SFFtools

=head1 SYNOPSIS

 	# create SFFfile factory
	my $factory = Bio::Tools::Run::SFFTools->new(
		-command => 'sfffile',
		-program_dir => '/usr/bin/sfftools/',
		-MIDsetname => 'gsMIDs',
		-MIDconfigFile => $MID_file,
        -output => $out_file,
		); 
            
	my $run = $factory->run(-in => $input_file);
	

=head1 DESCRIPTION

This is a wrapper for running BLAT, written by Jim Kent.


=head1 FILES

When a command requires filenames, these are provided to the
C<run()> method, not the constructor (C<new()>). To see the set of
files required by a command, use C<available_parameters('filespec')>
or the alias C<filespec()>:

If files are not specified per the filespec, text sent to STDOUT and
STDERR is saved and is accessible with C<$bwafac->stdout()> and
C<$bwafac->stderr()>.

=head1 FEEDBACK

=head2 Mailing Lists

User feedback is an integral part of the evolution of this and other
Bioperl modules. Send your comments and suggestions preferably to
the Bioperl mailing list.  Your participation is much appreciated.

  bioperl-l@bioperl.org                  - General discussion
http://bioperl.org/wiki/Mailing_lists  - About the mailing lists

=head2 Support

Please direct usage questions or support issues to the mailing list:

L<bioperl-l@bioperl.org>

rather than to the module maintainer directly. Many experienced and
reponsive experts will be able look at the problem and quickly
address it. Please include a thorough description of the problem
with code and data examples if at all possible.

=head2 Reporting Bugs

Report bugs to the Bioperl bug tracking system to help us keep track
of the bugs and their resolution. Bug reports can be submitted via
the web:

  http://bugzilla.open-bio.org/

=head1 AUTHOR - Ben Bimber

Email bimber -at- wisc -dot- edu

=head1 APPENDIX

The rest of the documentation details each of the object methods.
Internal methods are usually preceded with a _

=cut

# Let the code begin...
package Bio::Tools::Run::SFFTools;

use strict;
use warnings;

use Bio::Root::Root;
use Bio::Tools::Run::SFFTools::Config;

use base qw(Bio::Tools::Run::WrapperBase Bio::Root::Root);
use Bio::Tools::Run::WrapperBase::CommandExts;

our $program_name = '*sff';
our $use_dash = 1;
our $join = ' ';
our $default_cmd = 'sfffile';

=head2 new

 Title   : new
 Usage   : my $obj = new Bio::Tools::Run::SFFTools();
 Function: Builds a new Bio::Tools::Run::SFFTools object
 Returns : an instance of Bio::Tools::Run::SFFTools
 Args    :

=cut

sub new { 
    my ($class, @args) = @_;
    
    #set defaults
   	unless (grep /command/, @args) {
		push @args, '-command', $default_cmd;
		}
		
    my $self = $class->SUPER::new(@args);    
    
    return $self;
}

sub run { 	 
	my ($self, %args) = @_;
	$self->_run(%args);
	
	if ($args{'-out'}){
		$self->{'_output'} = $args{'-out'}; 
	};
	
	#return $self;
			 	
}


sub getSummary {
	my ($self, $file) = @_;
	
	unless ($self->{'stdout'}){
		return "No STDOUT generated by this command";	
	} 
	
	if ($file) {
		open (REPORT, '>', $file);
		print REPORT $self->{'stdout'}		
	}
	
	close REPORT;
	return $self->{'stdout'}
}


sub filesCreated {
	my $self = shift;

	if ($self->{'_command'} eq 'sfffile'){	
		my @lines = split("\n", $self->{'stdout'});
		my @newSFFs;
		my %summary;
		my $basename = $self->{"_output"};
		
		foreach (@lines)
		{
			chomp($_);
			if ($_ =~ /^\s+(.*):\s+(\d+)/){
				my $mid = $1;
				my $num = $2;
				$summary{$1} = $2;
				if ($2 != 0){
					push(@newSFFs, $basename . "." . $1 . ".sff" )
				}		
			}
		}
		
		return \@newSFFs;
	}
	
	if ($self->{'_command'} eq 'sffinfo'){		
		return $self->{'_output'};	
	}		
		
}



1;
