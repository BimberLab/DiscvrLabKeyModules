<customView xmlns="http://labkey.org/data/xml/queryCustomView">
    <sorts>
        <sort column="alias" descending="false"/>
    </sorts>
    <filters>
        <filter column="aliasEnabled" operator="eq" value="Y"/>
    </filters>
</customView>